<header class="masthead">
    <div class="container text-white">
        {% if template == 'frontoffice/pages/home' %}
        <div class="masthead-heading text-uppercase">GRÉGORY VANDEVRAYE</div>
        <div class="masthead-subheading">
            <p>Besoin d'un développeur back-end,</p>
            <p>c'est votre jour de chance, je suis là pour vous.</p>
        </div>
        {% else %}
            <div class="masthead-heading text-uppercase">{{ header_title }}</div>
        {% endif %}
        <a href="{{ url_domain }}cv/CV_VANDEVRAYE_GREGORY.pdf" target="_blank" class="cv-button btn btn-dark btn-lg mt-5 text-white">Télécharger mon CV</a>
    </div>
</header>
