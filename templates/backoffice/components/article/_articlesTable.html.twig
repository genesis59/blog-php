<table class="table table-responsive">
    <thead>
    <tr>
        <th class="col-3">Titre</th>
        <th class="col-5">Chapô</th>
        <th class="col-2">Dernière modification</th>
        <th class="col-2">Actions</th>
    </tr>
    </thead>
    <tbody>
    {% if articles is not null %}
        {% for article in articles %}
            <tr>
                <th>{{ article.title }}</th>
                <td>{{ article.chapo }}</td>
                <td>{{ article.updatedAt|date("d M Y à H:i") }}</td>
                <td>
                    <div class="btn-group mt-3">
                        <a href="{{ url_domain }}article/{{ article.slug }}" class="btn btn-primary">Voir</a>
                        <a href="{{ url_domain }}admin/article/edit/{{ article.slug }}" class="btn btn-primary">Modifier</a>
                        <button type="button" class="btn btn-danger modal-trigger" name="deleteArticle" value="{{ article.id }}">Supprimer</button>
                    </div>
                </td>
            </tr>
        {% endfor %}
    {% endif %}
    </tbody>
</table>
