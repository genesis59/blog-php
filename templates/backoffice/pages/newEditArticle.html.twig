{% extends './backoffice/layout.html.twig' %}

{% block content %}
    <h2 class="border-5 border-bottom border-dark pb-2">{{ is_new ? "Nouvel article" : "Éditer l'article" }}</h2>
    {% include './components/_flashes.html.twig' %}
    <div class="container p-5">
        {% if is_new == false and authors is null  %}
            Désolé, nous ne pouvons donner suite à votre demande.
        {% else %}
            {% include './backoffice/components/form/_newEditArticleForm.html.twig' %}
        {% endif %}

    </div>
{% endblock %}